#include <stdio.h>

int main() {
  float a,b,c,i,n;
  printf("enter");
  scanf("%f",&n);
  a=1;
  for(i=2;i<=n;i++){
      b=1/i;
      c=a+b;
      printf("%f\n",c);
      a=c;
     
  }

    return 0;
}
